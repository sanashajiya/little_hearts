import 'package:flutter/material.dart';

class AppColors {
  // =========================================================
  // CORE BRAND COLORS (Little Hearts Default)
  // =========================================================
  static const Color primary = Color(0xFF6E5BCB);
  static const Color primaryDark = Color(0xFF5746A8);
  static const Color primaryLight = Color(0xFF9A8CE0);

  // =========================================================
  // MODE-SPECIFIC COLORS
  // =========================================================

  /// Friend Mode (Brighter Blue)
  static const Color friendMode = Color(0xFF4F8BFF);
  static const Color friendModeLight = Color(0xFF86ADFF);
  static const Color friendModeDark = Color(0xFF2E67E8);

  /// Date Mode (Brighter Pink)
  static const Color dateMode = Color(0xFFFF5F8A);
  static const Color dateModeLight = Color(0xFFFF9BB4);
  static const Color dateModeDark = Color(0xFFE64570);

  /// Hangout Mode (Brighter Green)
  static const Color hangoutMode = Color(0xFF2FD06C);
  static const Color hangoutModeLight = Color(0xFF6EEA96);
  static const Color hangoutModeDark = Color(0xFF1FA855);

  // =========================================================
  // BACKGROUNDS
  // =========================================================
  static const Color backgroundLight = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF1B1B2D);
  static const Color onboardingBg = Color(0xFFC6B6E8);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // =========================================================
  // BUTTONS
  // =========================================================
  static const Color buttonPrimary = primary;
  static const Color buttonDisabled = Color(0xFFDAD3F2);
  static const Color buttonText = Colors.white;

  // =========================================================
  // TEXT COLORS
  // =========================================================
  static const Color textPrimary = Color(0xFF1F1F1F);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textHint = Color(0xFFB0B0B0);
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textLink = Color(0xFF4C7DFF);

  // =========================================================
  // NEUTRALS
  // =========================================================
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF9C9C9C);
  static const Color greyLight = Color(0xFFF3F3F6);
  static const Color greyDark = Color(0xFF5E5E5E);

  // =========================================================
  // BORDERS & DIVIDERS
  // =========================================================
  static const Color borderLight = Color(0xFFE6E6E6);

  // =========================================================
  // STATUS COLORS
  // =========================================================
  static const Color success = Color(0xFF3DDC84);
  static const Color error = Color(0xFFFF5252);

  // =========================================================
  // INDICATORS
  // =========================================================
  static const Color indicatorActive = Color(0xFF7A5CDA);
  static const Color indicatorInactive = Color(0xFFFFFFFF);
}
